# ============================================================================
# Databricks Local Emulator - Environment Configuration
# ============================================================================
# Copy this file to .env and customize the values for your environment.
# All variables are optional — sensible defaults are used when unset.
# ============================================================================

# ----------------------------------------------------------------------------
# Local Storage Paths (Optional)
# ----------------------------------------------------------------------------
# Override default paths for warehouse, volumes, and DBFS.
# Defaults: .warehouse/, .volumes/, .dbfs/ in the project root.

# LOCAL_WAREHOUSE=.warehouse
# VOLUMES_ROOT=.volumes
# DBFS_ROOT=.dbfs

# ----------------------------------------------------------------------------
# Spark Configuration (Optional)
# ----------------------------------------------------------------------------

# SPARK_DRIVER_MEMORY=2g

# ----------------------------------------------------------------------------
# Databricks Workspace Simulation (Optional)
# ----------------------------------------------------------------------------
# Simulate Databricks workspace settings for notebook context.

# DATABRICKS_CLUSTER_ID=local-cluster
# DATABRICKS_CLUSTER_NAME=databricks-local
# DATABRICKS_CATALOG=main

# ----------------------------------------------------------------------------
# Notes
# ----------------------------------------------------------------------------
# 1. Never commit .env to version control (already in .gitignore)
# 2. No Docker, MinIO, or PostgreSQL required — everything runs locally
# 3. Just run: pip install -e ".[dev]" && pytest tests/ -v
